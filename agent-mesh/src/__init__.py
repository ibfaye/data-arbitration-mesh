"""Autonomous Data Arbitration Mesh

A peer-to-peer multi-agent mesh that negotiates schema changes,
enforces regulatory compliance (CDP/GDPR), and produces auditable
decision ledgers — without a human in the loop.

Design principle: agents are computational peers, not org chart mirrors.
Governance is a shared Constraint Ledger, not a gatekeeper agent.
"""

__version__ = "0.1.0"
